# ONNX-to-SNN Compatibility Entry

本文件仅供仍自动读取 `CLAUDE.md` 的工具使用，不维护第二套规则。

开始工作前读取：

1. `AGENTS.md`
2. `NOW.md`
3. 与任务相关的 implementation、test、ONNX graph、diff 和真实结果

环境、命令和实验策略见：

- `docs/ENVIRONMENT.md`
- `docs/RUNBOOK.md`
- `docs/EXPERIMENT_POLICY.md`

只使用 `/Users/cvue/Documents/github_zr/spikingjelly`；不得使用禁用旧目录 `/Users/cvue/Documents/spikingjelly`。不要求固定 Agent 分工。允许挑战现有假设，但必须保持 canonical graph/structured ANN/SNN 技术边界，不得伪造引用、无证据 claim 或用过度防御性实现掩盖失败。
