# ONNX-to-SNN Environment

## Only Active Workspace

- Use: `/Users/cvue/Documents/github_zr/spikingjelly`
- Do not use: `/Users/cvue/Documents/spikingjelly`

The old directory is disabled for all reads, edits, tests, commits and generated artifacts. If a command, IDE or terminal points there, stop and switch to the active workspace.

## Git Remotes

- `origin`: `git@github.com:zhairui1995/onnx2snn-spikingjelly.git`
- `upstream` fetch: `https://github.com/fangwei123456/spikingjelly.git`
- `upstream` push is disabled.

## Python

Verified local interpreter path:

```text
/opt/homebrew/Caskroom/miniforge/base/envs/spikingjelly/bin/python
```

Use this interpreter for focused tests and `pip check` unless the user explicitly selects another validated environment.

Do not record credentials or private environment secrets here.
